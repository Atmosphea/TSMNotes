
import { useLocation } from 'wouter';

const Navigation = () => {
  // Navigation component is no longer needed as per user request
  // Empty component that doesn't render anything
  return null;
};

export default Navigation;
